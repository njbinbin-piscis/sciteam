# Role catalog

`CATALOG.yaml` lists professional posts for mission team composition.

- Prefer these posts when designing task teams (`plan.compose`, wizard, seeds).
- Never roster `worker`, `stage_*`, or other generic reusable labels.
- If a specialty is missing, add a catalog entry with `default_skills` —
  do not invent an anonymous agent.
