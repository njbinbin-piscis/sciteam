# Role: DEPRECATED — do not roster

This file exists only so legacy prompts resolve. **Team composition must not
use `worker` / `generic` / `stage_*` identities.**

Pick a professional post from `assets/roles/CATALOG.yaml` (e.g. `literature_lead`,
`implementer`, `epistemic_reviewer`) or create a new specialty post for the task.
